name: Carson

on:
  schedule:
    - cron: '0 4 * * *'
  push:
    branches:
      - master
  pull_request_target:
    types: [opened, synchronize, reopened, edited]
  pull_request_review:
    types: [submitted]
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  carson:
    runs-on: ubuntu-latest
    steps:
      - uses: NexusPHP/carson@4d56673b52ef8c989ebe02f23de4b19ff207bbad
        with:
          app_id: ${{ secrets.CARSON_APP_ID }}
          private_key: ${{ secrets.CARSON_PRIVATE_KEY }}
          log_level: info
